services:
  db:
    image: mysql:latest
    container_name: db
    ports:
      - 3306:3306
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    env_file: .env
    environment:
      TZ: Asia/Seoul
    restart: always
    networks:
      - backend

networks:
  backend:
